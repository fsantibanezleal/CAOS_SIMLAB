# Base runtime — the LIVE lane + core teaching engine. Kept deliberately small: this is also the wheel
# closure the browser (Pyodide) must load, so every dependency here costs cold-start time.
numpy>=1.26
simpy>=4.1
# Kept minimal on purpose (every line is browser cold-start weight). Mesa/Ciw are NOT pinned here even though
# the ABM scenarios DO run live on real Mesa 3: the worker loads mesa (+ pandas/scipy/networkx/sqlite3) and
# ciw at runtime via loadPackage/micropip — they are pinned in requirements-precompute.txt (also used by the
# local pipeline). OR-Tools is native (no WASM) so it is precompute-only; see requirements-precompute.txt.
