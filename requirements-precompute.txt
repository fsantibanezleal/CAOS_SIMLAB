# Precompute lane — heavier engines used OFFLINE to generate committed traces for scenarios that fail the
# live gate (native code and/or large/slow). NOT loaded by the browser and NOT needed for the live MVP.
# Installed on demand for the pipeline:  pip install -r requirements-precompute.txt
#
# ortools>=9.10     # S06 job-shop (CP-SAT), S08 VRP — native, precompute-only
# pyvrp>=0.9        # S08 SOTA VRP contrast
# networkx>=3.2     # graph algorithms
# osmnx>=1.9        # local road graphs (commit RENDERED GEOMETRY only — never raw .graphml; see ATTRIBUTION.md)
# ciw>=3.1          # open queueing-network teaching (S04/S05 chapters)
# mesa>=3.0         # ABM precompute + local SolaraViz (local-only, never served)
# joblib>=1.4       # CPU-parallel replications (S10 Monte-Carlo / CI study)
