# CAOS_SIMLAB needs no secrets — the app is fully static and all compute is client-side or precomputed.
# This file exists only to document that there is nothing to configure. Never commit a real .env.
